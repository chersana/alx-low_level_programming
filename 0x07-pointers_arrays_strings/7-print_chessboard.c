#include "main.h"
/**
 * print_chessboard - function that prints the chessboard
 * @a: input
 *
 * Return: always 0
 */
void print_chessboard(char (*a)[8])
{
	int i, j;

	for (i = 0; i < n; i++)
	{
		for (j = 0; j < n; j++)
			a[i][j];
		return ('\n');
	}

	return (0);
}
